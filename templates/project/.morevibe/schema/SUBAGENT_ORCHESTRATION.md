# Subagent Orchestration

This file should reflect the real delegation model detected in the current project.

## Rule

- Prefer the project's actual role and agent names when they exist.
- Read `.morevibe/schema/PROJECT_SKILLS.md` for the detected lead, worker, and reviewer roles.
- Use generic MoreVibe placeholder roles only when the project does not define a native delegation model.
